&this is an example program
let
	const MAX ~ 10;
	var n ~ Integer;
	var c ~ Char
in
	begin
		c ~ {L};
		n ~ 1;

		put(c);

		loop 
			putint(n)
		while (n < MAX)
			n ~ n + 1
		repeat;
		
		? n == MAX => put(c);
	end