&this is an example program
let
	const MAX ~ 10;
	var n ~ Integer;
	var c ~ Char
in
	begin
		c ~ {L};
		n ~ 1;

		put(c);
		getint(var n);
		eof();
		loop 
			getint(var n)
		while (n < MAX)
			n ~ n + 1
		repeat;

		while (n < MAX) 
			put(c)
		wend;

		? eof() = eof() => put(c);
	end